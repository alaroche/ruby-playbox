require 'spec_helper'

describe SecretSanta do
  before(:each) do
    secret_santa = SecretSanta.run
  end

  it 'should return an array of hashes'

  it 'should not have santa the same as receiver.'

  it 'should have each person as both a santa and a reciever'

end
